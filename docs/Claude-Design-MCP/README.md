# Claude Design MCP

Working folder for assets Claude generates or edits through the connected
ARTLIST service (wired up via the Model Context Protocol, MCP).

Inputs typically come from `docs/greg-composite/`. Approved outputs land here;
final optimized copies (WebP, sized for the page) ship from
`public/assets/images/`.

Nothing in this folder is served on ooos.ca.
