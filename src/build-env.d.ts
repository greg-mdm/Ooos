/** Stamped in by Vite's `define` at build time (see vite.config.ts).
 *  A short base-36 id, unique per build, used to cache-bust the watchlist
 *  embed so a deploy always reaches returning visitors. */
declare const __EMBED_BUILD__: string;
